#include <stdio.h>

int main(void) {
    double appleDensity;     // ความหนาแน่นของแอปเปิ้ล (g/cm³)
    double waterDensity;     // ความหนาแน่นของน้ำ (g/cm³)
    double waterHeight = 0;  // ระดับน้ำเริ่มต้น (cm)
    double step = 0.2;       // เติมน้ำทีละ 2 มิลลิเมตร (0.2 cm)
    double radius;           // รัศมีของแอปเปิ้ล (cm)
    int rounds = 0;          // จำนวนครั้งที่เติมน้ำ

    printf("รัศมีของแอปเปิ้ล (cm): ");
    scanf("%lf", &radius);

    printf("ความหนาแน่นของแอปเปิ้ล (g/cm^3): ");
    scanf("%lf", &appleDensity);

    printf("ความหนาแน่นของน้ำ (g/cm^3): ");
    scanf("%lf", &waterDensity);

    // ถ้าแอปเปิ้ลหนักกว่าน้ำ → จมแน่นอน
    if (appleDensity > waterDensity) {
        printf("แอปเปิ้ลหนักกว่าน้ำ จมแน่นอนครับ \n");
    } 
    else {
        printf("เริ่มจำลองการเติมน้ำทีละ %.1f cm (2 มม.)...\n", step);
        while (waterHeight <= 2 * radius) {
            rounds++; // นับจำนวนรอบที่เติม
            printf("รอบที่ %d: เติมน้ำจนระดับ %.1f cm\n", rounds, waterHeight);

            // สมมติว่าเริ่มลอยเมื่อระดับน้ำถึงครึ่งลูก
            if (waterHeight >= radius) {  
                printf("\n🍎 แอปเปิ้ลเริ่มลอยที่ระดับน้ำ %.1f cm\n", waterHeight);
                printf("ใช้เวลาเติมน้ำทั้งหมด %d รอบ\n", rounds);
                return 0;
            }
            waterHeight = waterHeight + step; // เติมน้ำเพิ่ม
        }
        printf("เติมน้ำจนท่วมแอปเปิ้ลแล้วแต่ยังไม่ลอย \n");
    }

    return 0;
}



